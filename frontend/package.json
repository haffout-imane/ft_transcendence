{
  "type": "module",
  "scripts": {
    "build": "tsc && cp -r src/assets dist/assets && cp -r src/styles dist/styles && cp src/index.html dist/index.html && cp -r src/fonts dist/fonts && cp src/server.js dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "fastify": "^4.22.2",
    "@fastify/static": "^6.9.0",
    "@fastify/compress": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.2.2"
  }
}
